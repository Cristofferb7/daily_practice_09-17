#include <stdio.h>
#include <stdlib.h>



//space ship thrust 

typedef enum {
    none = 0,
    low = 5,
    medium = 9,
    high = 12,
    maximum = 20,



} Thrust;

int main (){

printf("ready to go %d\n", none);
printf("take off  %d\n", maximum);
printf("entering the space %d\n", medium);
printf("ready in deep space %d\n", low);

}